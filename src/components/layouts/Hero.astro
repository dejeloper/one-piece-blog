---
interface Props {
  title?: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  image?: string;
}

const {
  title = "Mi propio One Piece",
  subtitle = "El camino de la busqueda de la libertad a traves de One Piece.",
  ctaText = "Últimos capítulos",
  ctaLink = "#chapters",
  image = "/images/preview.webp",
} = Astro.props;
---

<section id="home" class="py-20 px-4 animate-fade-in">
  <div class="max-w-6xl mx-auto grid md:grid-cols-2 gap-12 items-center">
    <!-- Texto -->
    <div class="text-center md:text-left">
      <h1 class="text-4xl md:text-6xl font-bold mb-6">
        {title}
      </h1>
      <p class="text-xl md:text-2xl text-[var(--muted-foreground)] mb-8">
        {subtitle}
      </p>
      <a
        href={ctaLink}
        data-astro-prefetch="hover"
        class="inline-block bg-[var(--primary)]/90 text-[var(--primary-foreground)] px-8 py-3 rounded-lg font-semibold text-lg transition-transform active:scale-95 hover:bg-[var(--primary)]"
      >
        {ctaText}
      </a>
    </div>

    <!-- Imagen -->
    <div class="flex justify-center md:justify-end">
      <img
        src={image}
        alt="Ilustración decorativa"
        class="rounded-lg shadow-lg max-h-[400px] object-cover"
      />
    </div>
  </div>
</section>
